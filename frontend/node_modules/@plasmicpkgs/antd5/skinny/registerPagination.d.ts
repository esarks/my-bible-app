import { Pagination } from "antd";
import React from "react";
import { Registerable } from "./utils";
type AntdPaginationProps = Omit<React.ComponentProps<typeof Pagination>, "pageSizeOptions"> & {
    pageSizeOptions?: {
        pageSize: number;
    }[];
    paginatedUrl?: (pageNo: number, pageSize: number) => string;
};
export declare function AntdPagination(props: AntdPaginationProps): React.JSX.Element;
export declare const paginationComponentName = "plasmic-antd5-pagination";
export declare const paginationHelpers: {
    states: {
        pageSize: {
            onChangeArgsToValue: (_: number, pageSize: number) => number;
        };
        startIndex: {
            onChangeArgsToValue: (currentPage: number, pageSize: number) => number;
        };
        endIndex: {
            onChangeArgsToValue: (currentPage: number, pageSize: number) => number;
        };
    };
};
export declare function registerPagination(loader?: Registerable): void;
export {};
