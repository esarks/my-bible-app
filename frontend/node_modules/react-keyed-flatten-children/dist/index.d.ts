import { ReactNode, ReactChild } from 'react';

declare function flattenChildren(children: ReactNode, depth?: number, keys?: (string | number)[]): ReactChild[];

export { flattenChildren as default };
